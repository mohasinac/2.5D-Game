// Re-export of the server-side Firebase Admin helpers. Server code should migrate
// to importing from this path; the original src/utils/firebase.ts stays for now.
export * from "../../utils/firebase";
